[pytest]
asyncio_mode = auto
norecursedirs = .* __pycache__ venv
filterwarnings =
    ignore:Scheduler can now be instantiated directly
    ignore:::aio_jobs
    ignore:::pkg_resources
    ignore:::aioresponses
    ignore:coroutine 'mock_stomp_client.<locals>.send_stomp_client' was never awaited
